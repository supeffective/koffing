{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Development Container",
      "type": "node-terminal",
      "request": "launch",
      "command": ".devcontainer/run up app",
      "cwd": "${workspaceFolder}"
    }
  ]
}
